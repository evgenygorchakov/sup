# chatterbox-tts сам пинит torch/torchaudio и тянет CUDA-колёса с PyPI — здесь
# синтез идёт на GPU, отдельный индекс не нужен.
chatterbox-tts
# resemble-perth (водяной знак, зависимость chatterbox-tts) импортирует pkg_resources,
# которого нет в setuptools 81+. Без пина сервер падает на старте.
setuptools<81
soundfile
flask
